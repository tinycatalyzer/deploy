repositories:
- name: jetstack
  url: https://charts.jetstack.io

releases:
- name: cert-manager
  namespace: cert-manager
  chart: jetstack/cert-manager
  version: v0.13.0
  values:
  # create a values.yaml file in the directory to pass environment necessary variables
  - "./values.yaml"
